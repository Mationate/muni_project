

<div class="container" id="loginForm">
  <div class="login-form">
    <div class="main-div">
      <div class="panel">
        <h2>Municipalidad - Olvidaste tu contraseña?</h2>
      </div>
      <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f| %>
        <%= render "devise/shared/error_messages", resource: resource %>

        <div class="form-group">
          <%= f.email_field :email, autofocus: true, autocomplete: "email", class:'form-control', placeholder:'Correo' %>
        </div>

        <div class="actions">
          <%= f.submit "Envíame las instrucciones", class:'btn btn-primary btn-sm' %>
        </div>
        <hr/>
        <%= render "municipalities/shared/links" %>
        
      <% end %>
    </div>
  </div>
</div>

